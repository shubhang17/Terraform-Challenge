# Copy to backend.hcl (gitignored) or pass these flags to terraform init.
# Do NOT create this bucket — it is pre-provisioned by CyberEd.

bucket       = "cybered-assessment-tfstate-150105760360-ap-south-1"
key          = "candidates/candidate-003/terraform.tfstate"
region       = "ap-south-1"
encrypt      = true
use_lockfile = true
